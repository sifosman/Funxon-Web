// WEB ONLY — deploy-web/.npmrc
// Keep this web workspace self-contained and prevent npm/pnpm from hoisting
// into the monorepo root or touching the mobile workspace.
ignore-workspace-root-check=true
