import { PlaceholderPage } from '../components/PlaceholderPage';

export default function CataloguePage() {
  return <PlaceholderPage title="Catalogue" description="Manage your catalogue items and packages." backTo={{ label: 'Account', href: '/account' }} />;
}
