import { PlaceholderPage } from '../components/PlaceholderPage';

export default function ActivityDashboardPage() {
  return <PlaceholderPage title="Activity Dashboard" description="Track all your recent activity across the platform." backTo={{ label: 'Account', href: '/account' }} />;
}
