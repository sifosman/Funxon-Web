import { PlaceholderPage } from '../components/PlaceholderPage';

export default function EventListPage() {
  return <PlaceholderPage title="Your Events" description="View and manage your upcoming events." backTo={{ label: 'Planner', href: '/planner' }} />;
}
