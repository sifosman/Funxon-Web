import { PlaceholderPage } from '../components/PlaceholderPage';

export default function QuoteDetailPage() {
  return <PlaceholderPage title="Quote Details" description="Review and manage this quote request." backTo={{ label: 'Quotes', href: '/quotes' }} />;
}
