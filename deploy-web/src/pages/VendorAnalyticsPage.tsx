import { PlaceholderPage } from '../components/PlaceholderPage';

export default function VendorAnalyticsPage() {
  return <PlaceholderPage title="Vendor Analytics" description="View profile views, quote requests, and performance." backTo={{ label: 'Account', href: '/account' }} />;
}
