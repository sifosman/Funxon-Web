import { PlaceholderPage } from '../components/PlaceholderPage';

export default function VendorPortfolioPage() {
  return <PlaceholderPage title="Vendor Portfolio" description="View this vendor's portfolio and offerings." backTo={{ label: 'Discover', href: '/discover' }} />;
}
