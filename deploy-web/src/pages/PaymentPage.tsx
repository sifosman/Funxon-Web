import { PlaceholderPage } from '../components/PlaceholderPage';

export default function PaymentPage() {
  return <PlaceholderPage title="Payment" description="Complete your payment via PayFast." backTo={{ label: 'Checkout', href: '/subscription-checkout' }} />;
}
